# Database (Neon PostgreSQL)
# Get this from your Neon dashboard: https://neon.tech/
DATABASE_URL="postgresql://username:password@hostname:port/database?sslmode=require"

# Authentication - Change these for production!
ADMIN_USERNAME="your_admin_username"
ADMIN_PASSWORD="your_secure_password"

# Next.js Security
# Generate a secure secret: openssl rand -base64 32
NEXTAUTH_SECRET="your_32_character_secret_key_here"

# App URLs (Vercel will auto-generate NEXTAUTH_URL in production)
NEXTAUTH_URL="http://localhost:3000"

# Optional: Node Environment (auto-set by Vercel)
NODE_ENV="development"
